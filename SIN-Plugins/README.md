# SIN-Plugins

Canonical bucket for OpenSIN-owned OpenCode plugins.

Current source: `global-opencode-config/plugins/`

Quickstart:
1. Keep the plugin sources self-contained.
2. Preserve the OMOC swarm behavior during migration.
3. Update bucket docs when the plugin contract changes.

Status: scaffolded, legacy source still exists unchanged.
