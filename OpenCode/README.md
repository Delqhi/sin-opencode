# OpenCode

Canonical bucket for the upstream OpenCode CLI/runtime codebase.

Current source: `packages/opencode/`

Quickstart:
1. Make code changes in the existing package tree while the reorg is in progress.
2. Treat this bucket as the future home for the OpenCode runtime surface.
3. Keep package-level tests and typecheck gates green.

Status: scaffolded, no code moved yet.
